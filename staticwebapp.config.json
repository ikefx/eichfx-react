{
  "routes": [
    {
      "route": "/**",
      "rewrite": "/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*", "/assets/*", "/favicon.ico"]
  }
}